<form class="" action="/<%= @book.id%>" method="post">
  <div>
    <label class="whatever">Title:</label>
  <input type="text" name="title" value="<%= @book.title%>" placeholder="hi">
  </div>
  <div>
    <label class="whatever">Body:</label>
  <input type="text" name="body" value="<%= @book.body%>" placeholder="hi">
  </div>

    <% if (@book.id)!=nil%>
      <input type="hidden" name="_method" value="PUT">
    <% end %>

  <input type="submit" name="" value="save book">
</form>
